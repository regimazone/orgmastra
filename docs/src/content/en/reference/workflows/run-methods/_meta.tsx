import { Tag } from "@/components/tag";

const meta = {
  start: ".start()",
  resume: ".resume()",
  stream: ".stream()",
  streamVNext: <Tag text="experimental">.streamVNext()</Tag>,
  watch: ".watch()",
  cancel: ".cancel()",
};

export default meta;
